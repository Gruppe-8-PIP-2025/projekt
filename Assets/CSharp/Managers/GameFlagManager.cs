using UnityEngine;

/// <summary>
/// GameFlagManager collects, organizes and makes accessible the game's various
/// persistent flags.
/// </summary>
public class GameFlagManager : MonoBehaviour
{

  /// <summary>
  /// Start is called once before the first execution of Update after the
  /// MonoBehaviour is created
  /// </summary>
  void Start()
  {

  }
}